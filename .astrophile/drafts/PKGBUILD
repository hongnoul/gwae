# Maintainer: hongnoul <FIXME email>
# DRAFT generated by astrophile: review deps + pkgver before pushing to AUR.
pkgname=gwae
pkgver=0.1.0
pkgrel=1
pkgdesc="gwae: niri's scrolling tiling for your CLI agents, in any terminal (daemon-free, MIT)"
arch=('x86_64')
url="https://github.com/hongnoul/gwae"
license=('MIT')
depends=() # FIXME runtime deps
makedepends=('cargo')
source=("$pkgname-$pkgver.tar.gz::https://github.com/hongnoul/gwae/archive/v$pkgver.tar.gz")
sha256sums=('SKIP') # FIXME: updpkgsums before publishing

build() {
  cd "$pkgname-$pkgver"
  cargo build --release --locked
}

package() {
  cd "$pkgname-$pkgver"
  install -Dm755 "target/release/gwae" "$pkgdir/usr/bin/gwae"
  install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
