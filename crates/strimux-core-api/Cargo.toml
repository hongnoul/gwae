[package]
name = "strimux-core-api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "strimux hot-reload boundary: stable types shared by the host and the hot-swappable core"

[lib]
crate-type = ["rlib"]
