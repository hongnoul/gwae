# Maintainer: hongnoul
# AUR PKGBUILD template. Filled in at first release.
pkgname=strimux-bin
pkgver=1.0.0
pkgrel=1
pkgdesc="niri's scrolling tiling for your CLI agents, in any terminal"
arch=('x86_64' 'aarch64')
url="https://github.com/hongnoul/gwae"
license=('MIT')
depends=()
source=("strimux-$pkgver-x86_64-unknown-linux-musl.tar.gz::https://github.com/hongnoul/gwae/releases/download/v$pkgver/strimux-x86_64-unknown-linux-musl.tar.gz")
sha256sums=('PENDING')

package() {
  install -Dm755 strimux "$pkgdir/usr/bin/strimux"
}
